# Prevent platform-specific optional dependencies - STRICT
optional=false
omit=optional

# Allow soft engine version warnings (Railway has 20.18.1, need 20.19.0)
engine-strict=false

# Prevent all optional packages and specify linux platform
force-platform=linux
force-arch=x64

# Performance
prefer-offline=true
fetch-timeout=60000
